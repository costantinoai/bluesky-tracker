# =============================================================================
# BLUESKY TRACKER CONFIGURATION
# =============================================================================
# Copy this file to .env and configure your settings
# The .env file is gitignored to protect your credentials

# -----------------------------------------------------------------------------
# BLUESKY ACCOUNT SETTINGS
# -----------------------------------------------------------------------------

# Bluesky handle to track (without the @ symbol)
# Example: yourhandle.bsky.social
BLUESKY_HANDLE=your-handle.bsky.social

# -----------------------------------------------------------------------------
# AUTHENTICATION (OPTIONAL - NOT REQUIRED FOR MOST OPERATIONS)
# -----------------------------------------------------------------------------
#
# IMPORTANT: App password is OPTIONAL and NOT REQUIRED for most features!
#
# The tracker works without authentication for:
# - Public profile analytics
# - Follower/following counts
# - Public post engagement metrics
# - Most dashboard features
#
# Authentication is ONLY needed for:
# - Private/protected accounts
# - Detailed notification tracking
# - Some advanced engagement metrics
#
# PRIVACY & SECURITY:
# - Your password is NEVER shared with any external provider
# - It is stored ONLY in your local .env file
# - It is used ONLY by this tool to authenticate with Bluesky's official API
# - The .env file is automatically excluded from git tracking
#
# To generate an app password (if needed):
# 1. Go to: https://bsky.app/settings/app-passwords
# 2. Create a new app password
# 3. Copy the password below (format: xxxx-xxxx-xxxx-xxxx)
# 4. You can revoke it anytime from the same settings page
#
# Leave empty or comment out if not needed:
BLUESKY_APP_PASSWORD=

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS
# -----------------------------------------------------------------------------

# Port for the web dashboard (default: 8095)
PORT=8095

# Database file location (default: /app/data/bluesky.db in Docker)
# For local development, use a local path like ./data/bluesky.db
DATABASE_PATH=/app/data/bluesky.db

# Timezone for scheduled data collection (default: Europe/Brussels)
TZ=Europe/Brussels
